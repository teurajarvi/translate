import React from "react";

/* We can use any type of JS object when passing
   the createContext default value as string 
   "englis". It can be also f.e. { color: "red" }
   of array [1,2,3] 
  */
export default React.createContext("english");
